export type Movie = {
  id: number;
  title: string;
  description: string;
  length: number; // mins
  manufacturer: string;
  // all other possible properties
};

export type Seat = {
  id: number;
};
